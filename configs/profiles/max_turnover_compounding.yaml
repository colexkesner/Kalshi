capital:
  cap_mode: percent
  cap_value: 60.0

fees:
  enabled: true
  assume_maker_fee: true
  assume_taker_fee_on_exit: true

sizing:
  mode: fractional_kelly
  kelly_fraction: 0.15
  max_contracts_per_order: 10
  max_order_cost_dollars: 18.0

risk:
  strategy_mode: MAX_CYCLES
  max_open_positions: 10
  max_per_market_notional: 18.0
  max_orders_per_market: 3
  min_net_edge_cents: 2
  min_profit_cents: 1
  send_price_in_dollars: true

