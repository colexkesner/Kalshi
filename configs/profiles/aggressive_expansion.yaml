capital:
  cap_mode: percent
  cap_value: 70.0

fees:
  enabled: true
  assume_maker_fee: true
  assume_taker_fee_on_exit: true

sizing:
  mode: fractional_kelly
  kelly_fraction: 0.20
  max_contracts_per_order: 12
  max_order_cost_dollars: 20.0

risk:
  strategy_mode: MAX_CYCLES
  max_open_positions: 12
  max_per_market_notional: 20.0
  max_orders_per_market: 3
  min_net_edge_cents: 1
  min_profit_cents: 1
  send_price_in_dollars: true

